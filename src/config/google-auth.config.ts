/** Credenciales públicas de Google Identity (OAuth Web Client). */
export const googleAuthConfig = {
  /** Client ID de Google Cloud → APIs y servicios → Credenciales. */
  clientId: process.env.GOOGLE_CLIENT_ID ?? '',
};
